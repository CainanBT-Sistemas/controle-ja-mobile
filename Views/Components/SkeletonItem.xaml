<?xml version="1.0" encoding="utf-8" ?>
<ContentView xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="controle_ja_mobile.Views.Components.SkeletonItem">

    <VerticalStackLayout Spacing="0">
        <Grid ColumnDefinitions="Auto, *, Auto" ColumnSpacing="10" Padding="20,12" BackgroundColor="Transparent">

            <Border Grid.Column="0" WidthRequest="44" HeightRequest="44" Margin="0,0,15,0" VerticalOptions="Center"
                    BackgroundColor="#1E2A3A" StrokeThickness="0">
                <Border.StrokeShape>
                    <RoundRectangle CornerRadius="22" />
                </Border.StrokeShape>
                <BoxView BackgroundColor="#2D3A4F" Opacity="0.5" x:Name="ShimmerIcon" />
            </Border>

            <VerticalStackLayout Grid.Column="1" VerticalOptions="Center" Spacing="6">
                <Border BackgroundColor="#1E2A3A" StrokeThickness="0" HeightRequest="14" WidthRequest="150" HorizontalOptions="Start">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="4" />
                    </Border.StrokeShape>
                    <BoxView BackgroundColor="#2D3A4F" Opacity="0.5" x:Name="ShimmerTitle" />
                </Border>
                <Border BackgroundColor="#1E2A3A" StrokeThickness="0" HeightRequest="10" WidthRequest="100" HorizontalOptions="Start">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="4" />
                    </Border.StrokeShape>
                    <BoxView BackgroundColor="#2D3A4F" Opacity="0.5" x:Name="ShimmerSubtitle" />
                </Border>
            </VerticalStackLayout>

            <VerticalStackLayout Grid.Column="2" VerticalOptions="Center" HorizontalOptions="End" Spacing="6">
                <Border BackgroundColor="#1E2A3A" StrokeThickness="0" HeightRequest="14" WidthRequest="80" HorizontalOptions="End">
                    <Border.StrokeShape>
                        <RoundRectangle CornerRadius="4" />
                    </Border.StrokeShape>
                    <BoxView BackgroundColor="#2D3A4F" Opacity="0.5" x:Name="ShimmerAmount" />
                </Border>
            </VerticalStackLayout>

        </Grid>
        <BoxView HeightRequest="1" Color="#121214" Margin="0" />
    </VerticalStackLayout>
</ContentView>