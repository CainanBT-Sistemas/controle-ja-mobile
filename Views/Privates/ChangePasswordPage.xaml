<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:controle_ja_mobile.ViewModels"
             xmlns:toolkit="http://schemas.microsoft.com/dotnet/2022/maui/toolkit"
             x:Class="controle_ja_mobile.Views.Privates.ChangePasswordPage"
             BackgroundColor="#001524" 
             NavigationPage.HasNavigationBar="False">

    <Grid RowDefinitions="Auto, *, Auto">
        <Grid Grid.Row="0" ColumnDefinitions="Auto, *, Auto" Padding="15,20,15,10">
            <ImageButton Grid.Column="0" Source="arrow_back_36dp.png" Command="{Binding GoBackCommand}" HeightRequest="24" HorizontalOptions="Start" Aspect="AspectFit">
                <ImageButton.Behaviors>
                    <toolkit:IconTintColorBehavior TintColor="White" />
                </ImageButton.Behaviors>
            </ImageButton>
            <Label Grid.Column="1" Text="Alterar Senha" TextColor="White" FontSize="18" FontAttributes="Bold" HorizontalOptions="Center" VerticalOptions="Center"/>
        </Grid>

        <ScrollView Grid.Row="1">
            <VerticalStackLayout Padding="20,30,20,20" Spacing="20">

                <VerticalStackLayout Spacing="5">
                    <Label Text="Senha Atual" TextColor="#A1A1AA" FontSize="14" FontAttributes="Bold"/>
                    <Border BackgroundColor="#1E2A3A" StrokeThickness="0" HeightRequest="55" StrokeShape="RoundRectangle 8" Padding="15,0">
                        <Entry Text="{Binding CurrentPassword}" IsPassword="True" TextColor="White" FontSize="16" BackgroundColor="Transparent"/>
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="5">
                    <Label Text="Nova Senha" TextColor="#A1A1AA" FontSize="14" FontAttributes="Bold"/>
                    <Border BackgroundColor="#1E2A3A" StrokeThickness="0" HeightRequest="55" StrokeShape="RoundRectangle 8" Padding="15,0">
                        <Entry Text="{Binding NewPassword}" IsPassword="True" TextColor="White" FontSize="16" BackgroundColor="Transparent"/>
                    </Border>
                </VerticalStackLayout>

                <VerticalStackLayout Spacing="5">
                    <Label Text="Confirmar Nova Senha" TextColor="#A1A1AA" FontSize="14" FontAttributes="Bold"/>
                    <Border BackgroundColor="#1E2A3A" StrokeThickness="0" HeightRequest="55" StrokeShape="RoundRectangle 8" Padding="15,0">
                        <Entry Text="{Binding ConfirmPassword}" IsPassword="True" TextColor="White" FontSize="16" BackgroundColor="Transparent"/>
                    </Border>
                </VerticalStackLayout>

                <Label Text="Sua nova senha deve conter pelo menos 6 caracteres." TextColor="#64748B" FontSize="12" HorizontalOptions="Center" Margin="0,10,0,0"/>

            </VerticalStackLayout>
        </ScrollView>

        <VerticalStackLayout Grid.Row="2" Padding="20,0,20,30">
            <Button Text="Atualizar Senha" Command="{Binding SavePasswordCommand}" BackgroundColor="#00E676" TextColor="#001524" FontAttributes="Bold" FontSize="16" HeightRequest="55" CornerRadius="12"/>
        </VerticalStackLayout>

        <Grid Grid.RowSpan="3" BackgroundColor="#CC000000" IsVisible="{Binding IsLoading}">
            <ActivityIndicator IsRunning="{Binding IsLoading}" Color="{StaticResource Primary}" HeightRequest="50" WidthRequest="50" VerticalOptions="Center" HorizontalOptions="Center"/>
        </Grid>
    </Grid>
</ContentPage>