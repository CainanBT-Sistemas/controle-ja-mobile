<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:controle_ja_mobile.ViewModels"
             x:Class="controle_ja_mobile.Views.Publics.LoginPage"
             x:DataType="vm:LoginViewModel"
             NavigationPage.HasNavigationBar="False"
             Title="Login">

    <ContentPage.Background>
        <RadialGradientBrush Center="0.5, 0.2" Radius="0.8">
            <GradientStop Color="#1E293B" Offset="0.0" />
            <GradientStop Color="#0F172A" Offset="0.4" />
            <GradientStop Color="#002118" Offset="1.0" />
        </RadialGradientBrush>
    </ContentPage.Background>

    <Grid>
        <Grid BackgroundColor="#CC000000" 
              IsVisible="{Binding IsLoading}" 
              ZIndex="99">
            <ActivityIndicator IsRunning="{Binding IsLoading}" 
                               Color="{StaticResource Primary}" 
                               VerticalOptions="Center" 
                               HorizontalOptions="Center"/>
        </Grid>

        <ScrollView>
            <VerticalStackLayout Spacing="25" Padding="30" VerticalOptions="Center">

                <VerticalStackLayout Spacing="5" Margin="0,0,0,20">
                    <Image Source="logo_controle.png" 
                           HeightRequest="100" 
                           Aspect="AspectFit" 
                           Margin="0,0,0,15"/>

                    <Label Text="Bem-vindo de volta!" 
                           TextColor="{StaticResource TextPrimaryDark}" 
                           FontSize="26" 
                           FontAttributes="Bold" 
                           HorizontalTextAlignment="Center"/>

                    <Label Text="Acesse sua conta para continuar."
                           TextColor="{StaticResource TextSecondaryDark}"
                           FontSize="14"
                           HorizontalTextAlignment="Center"/>
                </VerticalStackLayout>

                <Border Stroke="{StaticResource TextSecondaryDark}" StrokeThickness="1" 
                        StrokeShape="RoundRectangle 10" BackgroundColor="{StaticResource CardBackgroundDark}"
                        Padding="15,2">
                    <Entry Placeholder="E-mail" 
                           Text="{Binding Email}"
                           Keyboard="Email"
                           TextColor="{StaticResource TextPrimaryDark}"
                           PlaceholderColor="{StaticResource TextSecondaryDark}"/>
                </Border>

                <Border Stroke="{StaticResource TextSecondaryDark}" StrokeThickness="1" 
                        StrokeShape="RoundRectangle 10" BackgroundColor="{StaticResource CardBackgroundDark}"
                        Padding="15,2">
                    <Entry Placeholder="Senha" 
                           Text="{Binding Password}"
                           IsPassword="True"
                           TextColor="{StaticResource TextPrimaryDark}"
                           PlaceholderColor="{StaticResource TextSecondaryDark}"/>
                </Border>

                <VerticalStackLayout Spacing="15" Margin="0,10,0,0">

                    <Button Text="ENTRAR"
                            Command="{Binding LoginCommand}"
                            BackgroundColor="{StaticResource Primary}"
                            TextColor="#000000"
                            FontAttributes="Bold"
                            CornerRadius="10"
                            HeightRequest="50"/>

                    <Button Text="Não tem conta? Crie agora"
                            Command="{Binding GoToRegisterCommand}"
                            BackgroundColor="Transparent"
                            TextColor="{StaticResource TextPrimaryDark}"
                            FontSize="14"/>
                </VerticalStackLayout>

            </VerticalStackLayout>
        </ScrollView>
    </Grid>
</ContentPage>